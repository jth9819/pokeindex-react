import LoadingIcons from "react-loading-icons";

const SiteLoadingIcon = (props) => {
    return (
            <LoadingIcons.SpinningCircles fill="#000" speed={2} style={{marginTop: "20px"}}/>
    )
};

export default SiteLoadingIcon;
